<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="fp.info"> 
 	<select id="empEmailSelect" parameterType="String" resultType="String">
		select empPwd from emp_Info where empId=#{empId}
    </select>
    <select id="empInfoForSession" parameterType="String" resultType="fp.info.model.EmpInfoDTO">
       select * from emp_Info where empId=#{empId}
    </select>
    <!-- 매칭된 정보 가져오기 -->
    <select id="matchInfo" parameterType="map" resultType="fp.estimate.model.MoveEstimateDTO">
select B.* from(select rownum as rnum,a.* from
                (select X.*, Y.empName from move_est X, emp_info Y where X.empIdx = Y.empIdx and empName=#{empName}) A) B
                where rnum <![CDATA[>=]]>#{start} and rnum <![CDATA[<=]]>#{end}
    </select>
  </mapper>